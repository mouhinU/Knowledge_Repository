-- ============================================================
-- V20: 出卷质量评分精度提升 — quality_score 从 INT 改为 DECIMAL(5,2)
--   支持两位小数（如 80.58），为每节点独立打分与综合评分精细化提供基础
-- ============================================================

ALTER TABLE kb_exam_history MODIFY COLUMN quality_score DECIMAL(5,2);
